#include <string.h>
#include "main.h"

/**
 * _strlen - returns the length of a string
 * @s: the pointer of the string
 *
 * Return: the length of the string
 */
int _strlen(char *s)
{
	int len = strlen(s);

	return (len);
}
