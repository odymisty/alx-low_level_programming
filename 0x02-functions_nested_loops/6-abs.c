#include "main.h"

/**
 * _abs - compute absolute value
 *
 * @x: given value
 *
 * Return: Absolute value of integer
 */
int _abs(int x)
{
	if (x >= 0)
	{
		return (x);
	}
	else
	{
		return (-x);
	}
}
