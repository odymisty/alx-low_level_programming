#include <stdio.h>

/**
 * main - print single digits from base 10
 *
 * Decription: Prints digits between 1 and 9
 *
 * Return: Always 0 (success)
 *
 *
 */

int main(void)
{
	int a;

	for (a = 0; a < 10; a++)
		printf("%d", a);
		printf("\n");

	return (0);
}
